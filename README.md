<div align="center">
  <img height="60" src="https://img.icons8.com/color/344/javascript.png">
  <h1>Вопросы для подготтовки к собеседованию по JavaScript</h1>

---

<div align="left">
<h4>1.	Какие типы данных существуют в стандарте ECMAScript?</h4>
<p>В JS всего 8 типов данных. 7 примитивы и один ссылочный.
В JavaScript существует восемь типов данных: числа (number), строки (string), логические значения (boolean), null (отсутствие значение), undefined (не определенное значение), большие числа (BigInt), а также объект (object) и символ (symbol). В некоторых источниках 6 примитивов т.к  typeof(null) === "object"</p>

---

<h4>2. Что такое переменная, оператор, выражение, функция? Что такое унарный, бинарный, тернарный операторы?</h4>
<p>Переменная представляет собой идентификатор, которому присвоено некое значение.
Оператор – это символ(ы) или ключевое слово, благодаря которому производятся некоторые виды вычислений с участием одного или нескольких значений. Значения, располагающиеся слева и справа от оператора, называются операндами. Оператор с одним операндом называется унарным, с двумя – бинарным, с тремя – тернарным.
Выражение – это фраза языка программирования JavaScript, которая может быть вычислена интерпретатором для получения значения.
Функция JavaScript - это блок кода, предназначенный для выполнения определенной задачи. 
</p> 
<span> Унарная операция - x++ или ++x. </span>
<span> Бинарная операция - 3+4 или x*y. </span>
<span> Тернарная операция - a ? d : c. </span>

---

3.<h4> Что делают операторы =, ==, === ? В чем разница?</h4>
<span>= - присваивает определенное значение.</span>
<span>== - выполняет не строгое сравненени. Без сравнения типов.</span>
<span>=== - выполняет строгое сравнение. В первую очеред сравнивает типы указанных переменных. При разработке лучше использовать его.</span>

---

4.<h4> Расскажите про синтаксис условных конструкций.</h4>
<span> Конструкция switch-case</span>

```javascript
switch(x) {
  case 'value1':  // if (x === 'value1')
    ...
    [break]

  case 'value2':  // if (x === 'value2')
    ...
    [break]

  default:
    ...
    [break]
}
```

<p>Переменная x проверяется на строгое равенство первому значению value1, затем второму value2 и так далее.
Если соответствие установлено – switch начинает выполняться от соответствующей директивы case и далее, до ближайшего break (или до конца switch).
Если ни один case не совпал – выполняется (если есть) вариант default.</p>

<span> Конструкция if-else</span>

```javascript
if (условие) {
  блок выполняется при выполнении указанного условия
} else {
  выполняется если условие не выполнилось
}

```

<span> так же возможно указать дополнительные условия</span>

```javascript
if (условие) {
  блок выполняется при выполнении указанного условия
} else if (условие) {
   блок выполняется при выполнении указанного условия
  } else {
  выполняется если ни одно из указанных условий не выполнилось

}

```

<span> Терарный оператор</span>

```javascript
let abc = a ? b : c;
```

<p> Если a истинно, то abc = b, если ложно abc = c </p>

---

<h4>5. Расскажите про синтаксис различных видов цикла. Как досрочно выйти из цикла? Как перейти к следующей итерации?</h4>
   <span>цикл For</span>
   <span> синтаксис</span>

```javascript
for (let i = 0; i < 10; i++) {
 тело цикла
}
```

let i = 0; - инициализация счетчика.
i < 10; - условие выхода из цикла.
i++; - шаг цикла.

<span>цикл do_while</span>
<span> синтаксис</span>

```javascript
do {
  i += 1;
  console.log(i);
} while (i < 5);
```

<p> В блоке do указанно условие при котором цикл будет выполнятся. При истользовании цикла do_while цикл всегда выполпится хотя бы один раз. while (i < 5); - условие выхода из цикла.</p>

<span>цикл while</span>
<span> синтаксис</span>

```javascript
while (n < 3) {
 тело цикла
}
```

<p> while (n < 3) - условие при котором цикл выполняется.  </p>

<span> break; </span>

<p> Оператор позволяет прервать выполнение цикла</p>

<span> continue; </span>

<p> Оператор позволяет прервать выполнение и перейти к следующей итерации </p>

<span>цикл for...in</span>
<span> синтаксис</span>

```javascript
for (variable in object) {
  выражения;
}
```

<p>Оператор for...in проходит по всем свойствам объекта и выполнит указанные выражения для каждого отдельного свойства. Под капотом это цикл for.</p>

<span>цикл for...of </span>
<span> синтаксис</span>

```javascript
for (variable of object) {
  выражения;
}
```

<p>Оператор for...of создаёт цикл, проходящий по перечислимым объектам (включая Array, Map , Set, объект arguments и так далее), выполная блок кода для каждого итерируемого значения.</p>

---

<h4>6.	Расскажите синтаксис функций и функциональных выражений? В чем разница? Как понять, что мы имеем дело с функциональным выражением? Что такое именованные функциональные выражения и где доступно имя этого функционального выражения?</h4>

<span>Синтаксис объявления функции</span>

```javascript
function name (аргументы функции) {
  тело функции;
}
```

<P> Функции объявленные таким образом всплывают. Т.е их можно вызывать раньше объявления</p>

<span>Синтаксис объявления функционального выражения</span>

```javascript
анонимное функциональное выражение
const newFunction = function (аргументы функции) {
  тело функции;
}
именнованное функциональное выражение
const newFunction = function name (аргументы функции) {
  тело функции;
}
```

<p>Главное отличие функционального выражения от функции в том, что имя функции можно опустить для создания аннонимной функции. Так же функциональное выражение не поднимается(hoisting)Так же мы должны использовать функциональные выражения для надежного рекурсивного вызова функции.</p>

<span>Именованное функциональное выражение</sopan>

<p> - функция в которой имя локально и доступно только внутри её области видимости. Для анонимной функции, значением свойства name будет имя переменной. Это же касается стрелочных функций (в их случае там будет записано имя переменной, т.к. они всегда анонимные). </p>

---

<h4> 7.	Что является областью видимости переменных, объявленных при помощи var/let/const? Как работает const? </h4>
 <p> <br><b>var</b> - область видимости функции. Переменные объявленные через var всплывают, т. е доступны до объявления. При обращении к такой переменной мы получим undefined. Возможно изменить значение, так же можно объявлять несколько раз. В этом случае мы просто пререзапишем хранящееся в ней значение. <p>

```javascript
if (true) {
  var a = 1;
}
function foo() {
  var b = 10;
}
console.log(a);
// 1
console.log(b);
// ReferenceError: b is not defined
```

 <p><br><b>let</b> - область видимости блока, значение можно изменить. Не всплывает. Если объявить дважды вызовет ошибку.</p>
 <p><br><b>const</b> - область видимости блока, значение нельзя изменить. Не всплывает. Если объявить дважды вызовет ошибку.</p>

```javascript
if (true) {
  let a = 1;
  const b = 1;
}
console.log(a);
console.log(b);
// ReferenceError: a is not defined
// ReferenceError: b is not defined
```

---

<h4> 8.	Когда происходит инициализация переменных и функций, а когда присваивание значения? Что будет в переменной var/let до/после инициализации, но до присваивания? </h4>
<p> Инициализация означает, что переменная запущена в работу, ей присвоено значение. Если при объявлении переменной ей не было присвоено никакого значения, она будет иметь специальное значение undefined, до тех пор, пока ей не будет присвоено другое значение. <br>При обращении к переменной объявленной до инициализации мы получим undefined, так как переменные объявленные с помощью var всплывают. При обращении к let получим ошибку.  </p>

---

 <h4> 9.	Как создать новую переменную и записать туда число? Какие группы значений для чисел существуют? Какие специальные значения для чисел существуют? Как получить эти специальные значения? Как эти специальные значения ведут себя в математических операциях? </h4>
 <p> В современном JavaScript существует два типа чисел:
Обычные числа в JavaScript хранятся в 64-битном формате IEEE-754, который также называют «числа с плавающей точкой двойной точности» .
BigInt числа дают возможность работать с целыми числами произвольной длины.<br>
Кроме обычных чисел, существуют так называемые «специальные числовые значения», которые относятся к этому типу данных: Infinity , -Infinity и NaN .
Infinity представляет собой математическую бесконечность ∞. Это особое значение, которое больше любого числа.
Мы можем получить его в результате деления на ноль:
Или задать его явно:
alert( Infinity ); // Infinity
NaN означает вычислительную ошибку. Это результат неправильной или неопределённой математической операции, если где-то в математическом выражении есть NaN, то результатом вычислений с его участием будет NaN.
 </p>
 ---
 <h4>10.	Как явно преобразовать какую-либо переменную к числу? Когда происходит неявное преобразование к числу? </h4>
 <p>
  для преобразования строки в число используют функцию Number().
 </p>

<span>Пример</span>

```javascript
let a = "15"; //строка
a = Number(a); //преобразует переменную а в число
alert(typeof a); //выведет number
```

## <p>Если вы будете использовать в математических выражениях строки, то JavaScript автоматически преобразует переменные в числа.</p>

<h4>11.	Какие математические операции вы знаете? Что такое Math?</h4>
<p><b>Math</b> - встроеный объект доступный из любого мета программы, хранящий в своих свойствах и методах различные математические константы и функции. Math-не работает с чисоами bigInt.</p>
<span><b>Math.abs(x)</b> - Возвращает абсолютное значение числа.</span>
<span><b>Math.ceil(x)</b> - Возвращает значение числа, округлённое к большему целому.</span>
<span><b>Math.floor(x)</b> - Возвращает значение числа, округлённое к меньшему целому.</span>
<span><b>Math.max([x[, y[, …]]])</b> - Возвращает наибольшее число из своих аргументов.</span>
<span><b>Math.min([x[, y[, …]]])</b> - Возвращает наименьшее число из своих аргументов.</span>
<span><b>Math.random()</b> - Возвращает псевдослучайное число в диапазоне от 0 до 1.</span>
<span><b>Math.round(x)</b> - Возвращает значение числа, округлённое до ближайшего целого.</span>
<span><b>Math.sqrt(x)</b> -Возвращает положительный квадратный корень числа.</span>
<br>
<p>Это основные математические опреации, но стоит иметь ввиду что их значительно больше чем указано</p>
---

<h4>12.	Расскажите про parseInt, parseFloat? Для чего они нужны и как работают?</h4>
<p>Служат для чтения числа из строки. Если в процессе чтения возникает ошибка, они возвращают полученное до ошибки число.</p><br>
<p>Функция parseInt возвращает целое число, а parseFloat возвращает число с плавающей точкой</p><br>
<p>Функции parseInt/parseFloat вернут NaN, если не смогли прочитать ни одну цифру:</p>

```javascript
аlert(parseInt("a123")); // NaN,
```

 <p>на первом символе происходит остановка чтения.</p>
 <br>
 <p>Функция parseInt() имеет необязательный второй параметр. Он определяет систему счисления, таким образом parseInt может также читать строки с шестнадцатеричными числами, двоичными числами и т.д.</p>

---

 <h4>13.Как округлить число до целого? Как округлить число до 3 знака после запятой? Как округлить до ближайшего целочисленного вверх и вниз?</h4>
 <span><b>Math.round(x)</b> - Возвращает значение числа, округлённое до ближайшего целого.</span>
<span><b>Math.ceil(x)</b> - Возвращает значение числа, округлённое к большему целому.</span>
<span><b>Math.floor(x)</b> - Возвращает значение числа, округлённое к меньшему целому.</span>
<span><b>num.toFixed(3)</b> - возвращает число в виде строки с заданным числом цифр поле запятой</span>
<div>

---

<h4>14.	Что получается в результате преобразования всех остальных типов данных к числу?</h4>
<p>Для преобразования можно сделать с помощью функции Number().</p>
<span>Undefined => NaN</span>
<span>Null => 0</span>
<span>Boolean => Результат равен 1, если аргумент равен true. Результат равен 0, если аргумент равен false.</span>
<span>String => Для строкового численного литерала вернёт исходное число. Для не численного литерала вернёт NaN.</span>
<span>Object => Применяются следующие шаги: <br>
<b>1.</b> Вызвать ToPrimitive (входной аргумент, подсказка Number).<br>

<b>2.</b> Вызвать ToNumber(Результат(1)).<br>

<b>3.</b> Вернуть Результат(2).</span>

## <p>Symbol - уникальный и неизменяемый тип данных. При вызове функции Number а аргументом Symbol получим ошибку.</p>

<h4>15.	Как создать новую переменную с каким-нибудь строковым литералом?</h4>

<p>При создании строковой переменной мы можем использовать одинарные, двйные или обратные кавычки.<br>Ниже приведен пример</p>

```javascript
let str1 = "string";
let str2 = "string2";
```

<p>Обратные кавычки(Шаблонные литералы) - допускают использования выражений внутри</p>

```javascript
let a = "1";
let b = `${a} мая`;
alert(b); //выведет 1 мая
```

---

<h4>16.	Как вывести 3-й символ в этой строке?</h4>

```javascript
let a = "qwerty";
alert(a[2]); //выведет e
```

<h4>17.	Можем ли мы заменить этот символ? Если да, то как?</h4>
<p> Мы можем заменить указанный символ используя специальные методы - split и join.
<span><b>split</b> - разбивает строку на массив строк путем разбиения  указанной подстрокой.</span>
<span><b>join</b> -объединяет все элементы массива (или массивоподобного объекта) в строку.</span>

```javascript
let a = "qwerty";
let b = a.split("");
b[2] = "z";
a = b.join("");
alert(a); //выведет qwzrty
```

---

<h4>18.	Как явно преобразовать переменную к строке? Когда происходит неявное преобразование к строке?</h4>
<p>Преобразовать переменную к строке можно используя метод str.toString()</p>
<p>Неявное преобразование вызывает использование обычного оператора сложения, +, с двумя операндами, если один из них является строкой</p>

---

<h4>19.	Как посмотреть длину строки?</h4>
<p>Свойство length представляет длину строки.</p>
<span>Синтаксис: str.length</span>

---

<h4>20.	Как проверить есть ли в строке какое-нибудь слово (это выражение нужно будет использовать внутри if(...) )?</h4>
<p>Для поиска слова, символа или любой другой подстроки в строке  используют метод indexOf. Возвращает первый индекс, по которому данный элемент может быть найден или -1, если такого индекса нет.</p>
<span>Синтаксис:</span>

```javascript
let str = 'банан яблоко груша';
if(str.indexOf('яблоко')) {
  выполнится блок кода
}
```

</div>
```
